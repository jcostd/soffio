{{- $siteTitle := "My Soffio Site" -}}
{{- $siteURL := "http://example.com" -}}
{{- $siteDesc := "A static site generated by Soffio." -}}
{{ .XMLHeader -}}
<rss version="2.0">
  <channel>
    <title>{{ $siteTitle }}</title>
    <link>{{ $siteURL }}</link>
    <description>{{ $siteDesc }}</description>

    {{- range $id, $doc := .Docs }}
    <item>
      <title>{{ $doc.Title }}</title>
      <link>{{ $siteURL }}/{{ $id }}.html</link>
      <guid>{{ $siteURL }}/{{ $id }}.html</guid>
      {{- if $doc.Meta.description }}
      <description>{{ $doc.Meta.description }}</description>
      {{- end }}
      {{- if $doc.Meta.date }}
      <pubDate>{{ $doc.Meta.date }}</pubDate>
      {{- end }}
    </item>
    {{- end }}

  </channel>
</rss>
